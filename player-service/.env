# Player ranking service environment variables
# Port the player service listens on
PORT=5001

# Frontend and backend origins allowed for CORS (comma separated arrays are not supported by dotenv)
# The server code expects FRONTEND_URL and BACKEND_URL individually; set both if needed.
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:8080

NODE_ENV=development
